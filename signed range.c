
#include<stdio.h>
#include<stdlib.h>
int main()
{
    int a=INT_MIN;
    int b=INT_MAX;
    printf("signed range from %d to %d\n",a,b);
    short c=SHRT_MIN;
    short d=SHRT_MAX;
    printf(" short signed range from %d to %d",c,d);

    return 0;
}
